#!/bin/sh
. "${HOME}/.local/bin/sb-lib.sh" || exit 1

CLI="token-usage"
ONLY="opencode"

sb_on_left_click() {
    body=$("$CLI" --detail --only "$ONLY" 2>/dev/null) || {
        sb_notify "OpenCode Zen Usage" "failed to fetch"
        return
    }
    sb_notify "OpenCode Zen Usage" "$body"
}

sb_on_right_click() {
    sb_module_help "🟠" "OpenCode Zen Usage" "Left: details · Right: help"
}

sb_handle_buttons

"$CLI" --statusbar --only "$ONLY" 2>/dev/null || printf "E err"
